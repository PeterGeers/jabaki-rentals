{
  "version": "v1",
  "hooks": [
    {
      "name": "Run tests after task",
      "description": "Runs Vitest after completing a spec task to validate nothing is broken",
      "trigger": "PostTaskExec",
      "action": {
        "type": "command",
        "command": "cd frontend && npx vitest run --reporter=verbose"
      }
    }
  ]
}
